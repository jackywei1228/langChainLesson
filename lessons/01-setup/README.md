# Lesson 01 - Setup and First Call

## Goal
Verify the environment and make a minimal model call.

## Key Concepts
- Model configuration (baseURL, model, apiKey).
- Simple `invoke` on a chat model.

## Run
```
npm run dev -- lessons/01-setup/index.ts
```

## Notes
- Requires `.env` with `ZHIPU_API_KEY`.
